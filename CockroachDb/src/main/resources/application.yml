server:
  port: 9001

spring:
  datasource:
      url: jdbc:postgresql://localhost:26257/test?sslmode=disable&user=root
      username: root
      password:
     # driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    #open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
